<% if @accounts.size == 1
	 account = @accounts[0]
%>
To reset your <%= Configuration.get(:site_name)%> password, please click the link below (or copy and paste it into your browser):
  <%= account[:url]%>

Thanks for being part of the <%= Configuration.get(:site_name)%>!
<% elsif @accounts.size == 0 %>
Sorry, there are no active accounts associated with your e-mail address.
<% else %>
Multiple <%= Configuration.get(:site_name)%> accounts are associated with your e-mail address.
To reset the password for one of the accounts click on the link for that account.
<% for account in @accounts %>
  Account: <%= account[:identity].user_name%>
    <%= account[:url]%>
<% end %>
Thanks for being part of the <%= Configuration.get(:site_name)%>!
<% end %>
  
