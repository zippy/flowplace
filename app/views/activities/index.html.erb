<h1>Listing activities</h1>

<table>
  <tr>
    <th>User</th>
    <th>Type</th>
    <th>Activityable type</th>
    <th>Activityable</th>
    <th>Contents</th>
  </tr>

<% for activity in @activities %>
  <tr>
    <td><%=h activity.user_id %></td>
    <td><%=h activity.type %></td>
    <td><%=h activity.activityable_type %></td>
    <td><%=h activity.activityable_id %></td>
    <td><%=h activity.contents %></td>
    <td><%= link_to 'Show', activity %></td>
    <td><%= link_to 'Edit', edit_activity_path(activity) %></td>
    <td><%= link_to 'Destroy', activity, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New activity', new_activity_path %>
