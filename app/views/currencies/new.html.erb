<% @title='New Currency' %>
<%= render(:partial => 'currency_accounts/sub_nav') %>

<% form_for(@currency) do |f| %>
	<%= render(:partial => 'f', :object => f) %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', currencies_path %>
